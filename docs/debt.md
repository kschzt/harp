# Technical debt ledger

Living document; items leave by being fixed, not forgotten.
(Started 2026-06-11; updated through the MPE-input session, 2026-06-17.)

| # | item | severity | status |
|---|------|----------|--------|
| 1 | `harp-deviced.c` monolith (2,137 lines, 14 globals) | high | DONE — split into engine/state/session/panel + device.h (mechanical move, A/B-verified byte-identical render vs old binary); engine note/queue/ramp state now PRIVATE behind panic-grade API (`engine_all_notes_off`, `engine_note_off_if`, `evq_full`) |
| 2 | protocol client duplicated (probe vs shell runtime) | high | DONE — `host/client.{h,c}` (hello/identity, refs, snapshot, closure fetch/push, refset CAS, rid correlation); probe and shell both consume it. Net −700 lines |
| 3 | parsers never fuzzed (spec §16/T9 requires it) | high | DONE — fuzz/ targets for cbor/envelope/object/link/audio (harpcore framing), the §10 params-blob codec (fuzz-state, device/state.c), AND the §9.2 EVT-stream parser (fuzz-evt, device/session.c::handle_evt_msg — the deepest host→device attack surface; was the gap behind the earlier OVERSTATED "DONE" that listed only the harpcore framing parsers). fuzz-evt TU-includes session.c to reach the `static` handler + fuzz/evt_stubs.c resolves the engine/store surface (mirrors fuzz-state). libFuzzer+ASan smoke in CI (Linux), ASan corpus replay locally (Apple clang has no fuzzer runtime); gen-corpus seeds from canonical encoders incl. the evt etypes (ump/ramp/mod/transport/txn); scripts/abuse-test.sh slams a live daemon (T9-lite). Follow-up: continuous fuzzing (OSS-Fuzz-style) once public; wire fuzz-evt into the ci.yml fuzz-loop matrix (currently `cbor envelope object link audio state` — add `evt`) |
| 4 | shell: no reconnect on device replug; log-and-continue error paths; `staged*` naming is stale | medium | DONE — supervisor thread owns session lifecycle: hot-plug attach, unplug -> silence + 1 s retry, replug -> hello + bundle re-assert + stream (scripts/replug-test.sh proves it on hardware); staged* renamed bundle* |
| 5 | debt tracked informally | low | this file |
| 6 | §15.1 runtime/shell process split + multi-shell session sharing | medium | IN PROGRESS — scoped as full per-part multitimbral: one device, many shells. Spec model landed (0.3.6: §15.1 session sharing keyed by (vid,pid,serial), §15.2 state-controller vs multitimbral event group, audio owner). Plan + 7-phase build + 3-platform test matrix in `docs/multitimbral-plan.md`. Decisions: 16 per-part synths; params channel-scoped (NOT id-namespaced — param-map-hash unchanged); host registry + per-shell SPSC event rings merged by the single feeder (keeps TSan invariant); first-bind is audio owner, siblings silent. Item 2 (host/client.{h,c}) remains the stepping stone for the eventual daemon split |
| 16 | multi-device: one shared runtime singleton -> all instances grab device #1 | high | DONE — per-instance runtime; selection by USB vid:pid:serial (exact serial -> same-model fallback -> fresh-any -> never cross-model); reconnect pins the bound serial; bundle records usb-identity (key 5, additive). Verified on a two-board bus: scripts/multidevice-test.sh (all rules) + replug-test (rule 6). harp-probe `list`, `-d usb:SERIAL`; au-host `--instances N`; HARP_DEVICE_SERIAL override |
| 7 | free-running mode unexercised by any DAW path (no analog device yet) | low | deferred until a converter HAT / real analog device |
| 8 | recall-test transient failure after cold boot (one occurrence, error codes now logged) | watch | monitoring |
| 9 | sub-ms PDC accuracy: reported latency vs ring-occupancy mean differs by ≲1 block | low | acceptable; revisit with async transport work |
| 10 | macOS RT path is sync libusb at user QoS; async transfers + CoreAudio workgroups would cut latency floor | medium | DONE (async) — usb_io rebuilt on async libusb: dedicated event thread, always-pending IN transfers both pipes, fire-and-forget pacing slots, drain-on-stall gone by construction. Cushion 5 -> 2 blocks, full flood matrix 64-1024 green: **16 ms total reported latency at DAW blocks <= 256** (was 32 ms); offline bounce 2.5x faster. CoreAudio workgroup join: DONE via the AU shell (RenderContextObserver -> reader/pump/feeder join the host workgroup; VST3 still has no API for it) |
| 14 | device cross-thread state was "benign race" volatiles (formally UB, TSan-opaque) | high | DONE — C11 atomics with explicit orderings throughout (params, note, counters, fence, audio flags); two REAL races fixed: panel-thread echo could read `d->io` during session teardown (all io access now under send_mu), and the session thread's "now" param-set wrote render-owned ramp state (now routed through the event queue). TSan-clean on the Pi under production USB traffic: recall, offline render, realtime flood + panel knobs, snapshot/revert during streaming — zero reports. Golden byte-identical (atomics preserve behavior; note: a TSan/-O1 binary renders DIFFERENT floats — never capture oracles from instrumented builds) |
| 13 | block-64 event timing was structurally broken and unmeasured: ramp-thinning flush emitted stale-END ramps (~1100/s), and event timestamps could be born into already-paced ranges | high | DONE — gesture-aware pend flush (flush = "now" set after a silent pacing block); frontier cap (frame END bounded by read + target + headroom - dawBlock) makes the invariant strict at every block size: no event is ever born behind the pacing frontier. Block-64 floods: evt_late 0, ramp_late 0, fence_timeouts 0 |
| 11 | timing-test pitch-arrival spread sits exactly at its 5 ms limit (wall-clock realtime test) — occasional at-limit failures under load | watch | monitoring; tighten the engine or loosen the limit with data, not vibes |
| 15 | kBlock=128 experiment (lower the latency floor below 16 ms) | — | MEASURED AND DECLINED 2026-06-12: at pacing block 128 the stream gaps audibly until the cushion reaches 4 blocks = 512 samples — the SAME 10.7 ms of time-cushion as 2×256. The jitter bound of this stack is ~10.7 ms regardless of frame size; halving kBlock only trims the headroom term (16 -> 13.3 ms) while doubling frame rate, pacing traffic, and fence checks. The real lever is the cushion itself: device-side RT scheduling / deeper pipelining / better silicon (KR260). Revisit there |
| 12 | evt_late margin at block 256 (one late event per ~8 min of flood) | high | DONE — event pump (dedicated event->wire thread) + event fence (spec 0.3.4 §8.3.1: pacing frames carry an event-sequence the device must consume before rendering). Order is structural now, not a timing budget. Flood-verified: evt_late 0 over 3×120 s at block 256, ramp_late rate down 10×, zero added latency. fence_timeouts counter is expected overshoot noise (fence counts future-timestamped events); evt_late stays the truth metric |
| 17 | shell runtime ThreadSanitizer coverage | medium | DONE 2026-06-15 — `tools/tsan-host` statically links runtime.cpp into a TSan main image (dlopen'd plugins can't init TSan) and drives the real thread set (supervisor/feeder/pump/reader + libusb event thread + a simulated DAW audio + control thread, incl. getStateBundle round-trips). `scripts/tsan-shell.sh` builds (`-DHARP_TSAN=ON`) + runs it against a board. CLEAN across single (block 256), tight (block 64), and the TWO-LIVE-DEVICE run (PI4B-0001 + PI4B-0003, both streaming, claim contention) — `single/tight/multidev: clean`, TSAN-SHELL PASS. Not a cloud-CI gate (needs hardware) |
| 19 | offline golden render not gated in PR CI (the determinism oracle is hardware-suite-only) | medium | **DONE (#119)** — `tools/engine-golden.c` (`harp-engine-golden`) links `device/engine.c`'s `render_output()`/`engine_voices_cold()`/`evq_*` with NO USB, renders a fixed C-major sequence through the byte-identical golden path, FNV-1a hashes it, and asserts run-to-run determinism + non-silence + a per-OS pinned hash; wired into the `ci.yml` core job via `scripts/engine-golden-test.sh`. The companion gcovr coverage lane landed in #120. |
| 20 | `evq_apply_due` sample-accurate segmentation + `DEV_EV_MOD` per-voice addressing untested off-hardware | medium | deferred — extract the channel→part + mod-addressing predicate into pure helpers like `device/arp_select.h` / `device/voice_alloc.h` (both now host-unit-tested in `tests/harp_engine_logic_tests.c`), then unit-test in CI. Harder: render-thread statics |
| 21 | hw runner (PI4B-0002) intermittent device flakiness | watch | ROOT-CAUSED 2026-06-23 — the recall / getState / session-share family of failures was the store-bloat **recall-breaker (#22)**, not a transport flake: a store with thousands of §11.4 archives made `state.refs` overrun the §4.2.1a 64 KiB CTL cap, so recall failed deterministically once the rig's store had grown large enough (and a fresh store ran 16/1 green). Fixed by #22. The remaining pattern is the group-underrun RMS inversion (alias-play: sink-rms ≥ main-rms) — a passthrough multi-instance underrun, NOT a logic regression (a same-code re-run goes green; passed at 4f562f6); being moved to loopback like alias-part-audio. The rapid-claim gadget wedge stays a harness inter-test-recovery concern |
| 22 | device state-store grows unbounded → recall breaks on a bloated store | high | DONE — PI4B-0002 had bloated to 525 MB / ~121 K objects / 9,496 refs: every §11.4 recall archives the prior live ref (`archive/<ts>`) and NOTHING reclaimed them. Two distinct bugs. **(a) the recall-breaker:** `state.refs` returns the WHOLE ref list in ONE control message; past ~1,300 refs it exceeds the §4.2.1a 64 KiB CTL payload cap → the host's recv rejects it (−2 malformed → session reset §12.4) → getState / recall / session-share all fail. FIXED (fix-i, #33): `state.refs` takes an optional `{0: name}` filter and `harp_client_find_ref` resolves a single ref by name, so recall reads the live ref directly regardless of archive count (`scripts/bloat-recall-eth-test.sh` proves resolution at 1,500 refs). **(b) the bloat itself:** no retention, no GC. FIXED (this PR, #22a): the device retains only the newest `HARP_ARCHIVE_KEEP=32` refs per archive/duplicate namespace (oldest-first lexicographic) and runs a bounded, fail-closed mark-sweep (`harp_store_gc`, `include_parents=false` so superseded parent history is reclaimable) every `HARP_GC_INTERVAL=32` archive-class refsets — the whole cycle under `state_mu`, which also closes a two-thread put-then-ref race with the panel's `do_snapshot`. `test_store_gc` (unit: closure-preserved + fail-closed + bounded-sweep + crash-resume) + `scripts/gc-test.sh` (e2e: 40→32 retention, orphan parent history reclaimed, live still resolves). Misdiagnosis trail (a day lost): autosuspend / USB-device-passthrough / VM reboots all chased and all wrong — reproducing the getState failure over a CLEAN Ethernet path exonerated the rig and pinned it on the store |
| 18 | harp.local (PI4B-0001) harp-deviced SIGSEGV | high | FIXED 2026-06-15. NOT the FFS/USB path — a stack buffer overflow in the panel `refs` responder (`device/panel.c`). `panel_refs_cb` accumulated into a fixed 4096-byte stack `body` via `off += snprintf(...)`; snprintf returns what it WOULD write, so once the refs no longer fit, `off` ran past `sz`, `sz - off` underflowed to a huge size_t, and the next entry wrote past the buffer and smashed the stack (the saved regs in the core were JSON text). Data-dependent: overflowed at ~48 refs; harp.local the desk board had 101 (`live/project` + 100 archive snapshots), CI boards a handful -> "1 worked, CI fine, regression over time". Captured via a service-mode core dump (the systemd restart-loop reliably re-triggered macOS enumeration -> the sidecar re-polled `refs` -> crash). Fix: refs build into a growable heap buffer (`refs_appendf`, underflow-proof, returns the full list instead of truncating); deployed to harp.local, 101 refs now return as valid JSON, service stable (NRestarts=0). Unblocks #17's two-live-device run |

## Spec-conformance gaps (bar §13)

A separate axis from the engineering-debt table above: these are normative HARP-0.3
MUST/SHOULD requirements found unimplemented or stubbed by the adversarial spec-vs-impl
audit (2026-06-21; see the `harp-spec-conformance` note). The audio/state/control core
is mature and genuinely well-tested with zero rubber-stamped stubs — what is tracked
here is the normative *periphery*. Closed one at a time.

| spec | gap | status |
|------|-----|--------|
| §10.2 | unknown hash-algorithm byte silently accepted on read (a 33-byte string with any leading byte was taken as a valid hash) | DONE — central `harp_hash_read()` validates length+algorithm at all 8 wire parse sites (object/store/session/client); unit (`test_hash_read`) + e2e (cas-test case d: a bad-alg expect hash -> `malformed`, before any conflict/closure check) |
| §14.2 | diag counters emitted as constant 0: usb_errors, audio_underruns, audio_late_frames, msc_discontinuities, clock_drift_ppb | DONE — **usb_errors** WIRED (abnormal FunctionFS transport errors via `g_usb_errors` in ffs_link.c; 0 on the TCP/eth link which has no FFS; ffs-link unit test asserts the 0→1 move). **audio_underruns / msc_discontinuities / clock_drift_ppb** documented HONEST-0 with spec-backed reasons (no input buffer to starve — stall side is audio_overruns; MSC monotone by construction; drift is host-side per §7.3, device IS the clock source). **audio_late_frames** WIRED (§8.2: host_paced_loop tracks the SSI render cursor; a pacing frame whose ts is behind it is discarded + counted — the golden's SSI is monotone so it never fires; `harp-eth-latefr-test` asserts 0→1). All six §14.2 counters now honest |
| §7.1/§8.6 | `time.epoch` ntf never emitted; device epoch was 0 in the normal daemon path (the `=1` init is `--rtp-out`-only); stale-epoch discard + `evt_stale_epoch` missing | DONE — epoch starts at 1 and increments on a sample-rate change, emitting `ntf time.epoch {new,rate,old,old-msc-final}` (old-msc-final = the audio thread's published final MSC); stale-epoch H→D events discarded + counted (`evt_stale_epoch`, now real in diag.counters). e2e `harp-probe epoch-test` (rate 48000→44100 → time.epoch new=2/old=1; a stale epoch-1 event → evt_stale_epoch 0→1), CI-gated |
| §5.4 | host never surfaces a firmware/host-update prompt on `incompatible` (collapses all errors to EDEV); device sends only a static string, no machine-readable supported-range | DONE — device 'incompatible' carries supported major range (details key 2 = {0 min, 1 max}); host parses it + returns distinct `HARP_CLIENT_EINCOMPAT`; shell sets `needsFirmwareUpdate_` + logs specifics. e2e `harp-probe version-test` (forced `HARP_FORCE_PROTO_MAJOR` mismatch -> incompatible+range; control hello succeeds), CI-gated |
| §5.5 | `core.changed` absent; `core.bye` handler has no sender; `core.identify`/`core.ping` have no host caller | DONE — the device handlers existed; this adds the missing host callers + the one missing device sender. `harp_client_identify`/`ping`/`bye` in the shared client: `core.identify` re-fetches the bare identity map with NO session reset; `core.ping` echoes a per-call nonce (liveness); `core.bye` orderly-end caller exists (exercised by core-test) but is NOT wired into the shell teardown — auto-bye on every `sessionDown` regressed the USB hw suite (the device session-close is the FunctionFS "host sees disconnect" path, so it slowed/disrupted the per-test re-claim; loopback hid it, the USB rig caught it), so shell-auto-bye is deferred until USB-safe. NEW device sender `ntf_core_changed(topic)` emits `core.changed {0:tstr}` (the generic "re-query X" hint), driven via the `x.harp-refdev.notify-changed` conformance seam (the refdev has no spontaneous identity change); the host records the hint (`changed_pending`/`last_changed_topic`) + forwards to on_ntf. 6-lens adversarial review: 0 blockers; 1 should-fix = bound the teardown ctl recv with SO_RCVTIMEO (a PRE-EXISTING `audioStopLocked` hazard `core.bye` barely widens — tracked as a follow-up). Test: `harp-probe core-test` + scripts/core-test.sh (ping-echo / identify-matches-hello / changed-received / bye-acked), in eth-suite |
| §12.1 | NEGOTIATED state never entered (the flow jumped DETACHED→ATTACHED→SYNCED) | DONE — sessionUp records DETACHED→ATTACHED (transport up) → NEGOTIATED (hello-ok) → SYNCED (audio.start) → STREAMING; diag-bundle-host asserts NEGOTIATED appears in the session-history ring |
| §12.2 | serial-differs "different unit" + engine-major read-only flows are reason-code-only (SERIAL_MISMATCH/ENGINE_MAJOR_MISMATCH enum, no path) | DONE (engine-major) — helloAndIdentity parses the device engine major; a change across (re)connect records an ENGINE_MAJOR_MISMATCH transition and sets readOnlyDefault_, which holds the staged project state read-only (sessionUp skips the connect-time auto-push). HARP_FORCE_ENGINE_MAJOR seam + scripts/engine-mismatch-eth-test.sh proves the forced mismatch logs + records reason 4, with a control run guarding against false positives. SERIAL_MISMATCH stays enforced upstream: selectDevice pins boundSerial_, so the runtime never binds a different unit (a deliberate same-pin-or-nothing policy, not an unhandled path) |
| §4.2.1 | credit/flow-control is a no-op MUST violation (16 MiB granted up front, send fires unconditionally); ≤64KiB/≤4KiB ctl/evt/log bounds unenforced | DONE — (a) per-stream MESSAGE bounds DONE: harp_link_recv enforces ctl 64KiB / evt 4KiB / log 4KiB during reassembly (over-cap → -2 malformed → session reset §12.4; also bounds the accumulator against a no-FIN flood). OBJ stays uncapped (credit-gated). HARP_{CTL,EVT,LOG}_MAX_PAYLOAD in frame.h; test_link_stream_caps. Verified no live message trips it (largest ctl ~3KiB diag.bundle; evt/log are single events/records; bulk rides OBJ). (b) credit ENFORCEMENT DONE: obj sends now respect peer_credit — the overflow is queued (FIFO ring of hashes, cap 1024) and flushed when a core.credit grant arrives; the host gained the missing sliding-window re-grant-on-consume (symmetric to the device's handle_obj). Deadlock-free: grants ride the ungated CTL stream, the device recv thread enqueues (never parks), and the host self-pumps inside push_closure (no background thread raises peer_credit — pollEcho try-locks the held ctlMutex_ and drops ctl ntfs). Overflow = drop+count (obj_drops, kept out of emit_counters to preserve the §14.4 16-pair golden). HARP_FORCE_CREDIT_GRANT seam + scripts/credit-eth-test.sh recalls a 3.3 KiB closure byte-identically through a 1500 B window (queue+flush+re-grant, in order, no loss, no deadlock). Design via a 3-designer panel + judge; residual: oversize-single-object (> a whole grant) and silent-but-alive-peer stalls (same exposure as fetch_closure today, SO_RCVTIMEO-mitigated) — both documented, out of scope for the refdev |
| §9.8 | `evt.format`/`evt.parse` RPCs absent (shell formats params from a hard-coded table) | DONE (device) — device implements `evt.format {0 param-id, 1 float}→{0 tstr}` + `evt.parse` (inverse): a stepped param's value maps to its enum label (same mapping as the engine), a continuous param to a fixed-decimal, round-trip stable. `harp-probe format-test` gates it. SHOULD; the shell still uses its hard-coded table — the device RPCs are the §9.8 host fallback for mappings a descriptor can't express |
| §4.4.3 | mDNS/DNS-SD `_harp._tcp` discovery entirely absent (host dials a hardcoded `HARP_ETH_DEVICE`); TXT proto/port + serial-omission privacy unimplemented | DONE — DEVICE (`harp-deviced --mdns`, --port mode) advertises `_harp._tcp` via the platform responder (avahi on Linux, dns-sd on macOS) as a best-effort fork+exec, killed on shutdown so the responder emits the mDNS goodbye (§12.3). TXT carries `proto`=major.minor + `port`; the instance name is advisory and NO serial is published (§16 — identity comes via core.hello). HOST `harp-probe discover` browses + resolves via dns_sd (native macOS, avahi-compat libdns_sd on Linux; CMake-gated to a stub elsewhere — no build break), prints host:port + proto and FLAGS a serial-leaking TXT. Verified locally (advertise→discover→dial the resolved .local addr→live device) AND on the KR260 bench (avahi over the direct cable). scripts/mdns-discover-test.sh runs the round-trip in eth-suite on macOS (live mDNSResponder); Linux/Windows CI skip (no reliable responder), bench-proven instead. Discovery-only — the host may still dial a fixed HARP_ETH_DEVICE. Runtime auto-discovery integration (system device list) is a follow-up |
| §8.4 | ≥8 concurrent sessions each with own correlation/ASRC (one runtime owns one transport); admission-control / refuse-with-budget | DONE — ADMISSION CONTROL: a process-global, per-transport-PATH bandwidth ledger in runtime_registry (ledger_reserve/release/reserved, own mutex). audioStart reserves the session's audio bandwidth (out-union + in cols × 4B float32 × rate) BEFORE audio.start reaches the wire and REFUSES with the computed budget (requested/available/capacity logged + recorded ATTACHED→DETACHED) if the path is full — never degrades silently. Released on every exit (sessionDown, failed-audio.start, re-neg drops-own-row-first). Capacity: HARP_ADMISSION_BUDGET seam wins, else USB practical-bulk (High-Speed 30 MB/s spec-anchored), else ~110 MB/s eth nominal (declared policy — no controller is host-observable from a TCP socket). ≥8 SESSIONS: already structural (registry keys per-serial → independent runtimes/clocks/ASRC; admission caps bandwidth, never the count). UTILIZATION: diag-bundle top key 14 {path,reserved,capacity,permille} (bundle v3→v4; §16 clears the path-id). Tests: registry-tests (aggregation/refuse-with-budget/exact-release/re-neg-idempotency/no-leak/8-sessions) + scripts/admission-eth-test.sh (e2e refuse-vs-admit, renders differ) + diag-bundle-host v4 key-14 assertion. Design panel + judge. Residuals (documented): eth capacity is a declared policy number; cross-PROCESS aggregation needs the deferred §15.1 daemon; control/event-plane bytes unmetered (audio dominates) |
| §9.6 | transaction plane (etypes 2/3/4 silently skipped; no txn capability) | DONE — event transactions: etype 2 txn-begin {id} / 3 txn-commit {id, ?tstamp} / 4 txn-abort {id}. param/ramp/mod events carrying body key 4 (txn-id) BUFFER in a recv-thread-private table (DEV_TXN_MAX=4 concurrent × DEV_TXN_EVENTS_MAX=256 events) instead of applying; commit re-stamps the whole batch to one instant and applies it ALL-OR-NOTHING (new evq_push_batch — a per-event push could partially drop at DEV_EVQ_CAP and break atomicity); abort discards. Deferred-dirty: a buffered param/ramp does NOT live_ref_touch until commit. Capability "evt.txn" + limits in identity key 13 {0:max-concurrent, 1:max-events} (NOT baked into the cap string — the host drops caps ≥32 chars); host parses key 13. Over-limit/unknown/duplicate → reject + internal counter (evt_txn_rejected/overflow/unknown, kept out of the §14.2 16-pair golden, obj_drops precedent → zero golden regen). Transport (etype 7) excluded — its key 4 is PPQ (§9.7), and §9.6's use-cases are param/mod state. Table cleared at hello + session-start AND on every stream (re)start/stop (a buffered txn must not splice OLD-timeline instants into a new SSI epoch — same invariant as evq_reset_for_new_stream). txn-id 0 is RESERVED as the untagged sentinel (begin 0 rejected) so an honest peer can't silently lose atomicity by "opening" it. evq sized with headroom over a maximal txn (DEV_EVQ_CAP=512 ≥ DEV_TXN_EVENTS_MAX+64, _Static_assert) so a full 256-event commit stays committable on a live stream; a queue-full commit lands NOTHING and does NOT dirty the live ref (no phantom state.changed). Reject meters observable via a golden-free x.harp-refdev.txn read. Design panel + judge, then 6-lens adversarial review (1 blocker + 4 should-fix, all closed). Test: harp-probe txn-test + scripts/txn-test.sh — buffered≠applied, single + MULTI-event commit-atomic, abort-discards, unknown no-op, begin rejects (over-limit/duplicate/id-0) + per-txn overflow each counted via the meter, limits reported |
| tests | untested-but-implemented: §5.2 ignore-unknown-notification, §4.2 per-stream interleave, §11.5 storage counters, §10.4 snapshot rsp hash/gen | PARTIAL — §5.2 (`harp-probe notif-test`: an unknown NOTIFICATION is ignored + the device keeps serving — the audit's one status downgrade) and §4.2 per-stream interleave (`test_link_interleave`: two streams' interleaved frames demux independently) now covered + CI-gated. REMAINING: §11.5 storage-counter + §10.4 snapshot hash/gen value assertions |

**Audit status (2026-06-22).** The 2026-06-21 adversarial spec-vs-impl gap list is closed
except for the `tests` row's last two value-assertions (§11.5 storage counters, §10.4 snapshot
hash/gen). **§5.5** (the `core.changed` sender + the `core.bye`/`core.identify`/`core.ping`
host callers) is now DONE — the device-side handlers already existed; the host callers + the
`core.changed` sender + a conformance test were added (the one remaining qualifier is a
follow-up to bound the teardown ctl recv, a pre-existing hazard). Every
other row is DONE — some with a deliberately scoped qualifier (§9.8 device-side; §12.2
engine-major, with serial-mismatch enforced upstream by the same-pin policy). The remaining
qualifiers the rows already call out are RESIDUALS scoped out of the reference device, not
open MUST/SHOULD violations: §8.4 eth capacity is a declared policy number (no controller is
host-observable from a TCP socket) and cross-PROCESS aggregation awaits the deferred §15.1
daemon; §4.2.1 oversize-single-object (> a whole grant) and silent-but-alive-peer stalls; the
shell's hard-coded format table (§9.8 is a SHOULD — the device RPCs are the host fallback).
Separately, a few behaviors were implemented-but-untested rather than ever-missing and were
never audit gaps: the §8.3 120 dB ASRC-quality target and the Pull/Read-only/Duplicate recall
paths. The only structural bar left is **§13 firmware/cert** (device firmware + cert-grade
clock work), tracked separately.

Observed 2026-06-21 on the real KR260 over the `eth1` direct link (two genuinely
independent clocks, which localhost loopback cannot exercise): the §8.7 free-running
LIVE path holds transport bit-clean (0 loss / 0 underflow, 1507 pkts/run) and recovers
2–9 ppm of real cross-clock drift, at 43–63 dB SINAD (jitter-limited, 70–175 µs). The
host-paced bit-exact path is the fidelity path and is drift-independent by construction.
Not a conformance gap; recorded for the §8.3 ASRC-quality (SRC_SINC_FASTEST) discussion.
